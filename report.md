### PA0

#### 代码逻辑
1. 画线逻辑：
	- 使用bresenham算法来进行画线
	- 将曲线分成斜率绝对值大于1和小于等于1的部分；若斜率绝对值大于1，那么将x,y坐标进行互换再进行bresenham算法
	- 需要考虑xA,xB,yA,yB之间的相对大小，划线的方向始终是从A点到B点
	- x的变化方向是从$xA \rightarrow xB$,y的变化方向是从$yA \rightarrow yB$
	- 指标的比较变成绝对值是否大于0.5
2. 画圆逻辑
	- 先将原点平移到0点处，调用SetPixel的时候再平移回原先的原点
	- 从(0,radius)画1/8的圆，然后使用的颜色为该处点的color，剩下的1/8使用对称的形式得到
3. 区域填充
	- 与书中不同的是，本代码所使用的是队列，但是本质上不影响填充算法
	- 选取当前点为种子(seed)结点，放入queue中，然后填充对应的一行，直到遇到不同颜色的pixel，设置最左和最右的点的x轴坐标(xl,xr),然后分别填充上面和下面的行，将每一连续的区间中的最右点作为seed插入queue
	- 算法运行直到queue为空

#### 代码参考
1. 没有和同学进行讨论；没有借鉴网上的和别的同学的代码

#### 遇到的问题
1. 越界填充
	- 在flood fill的时候没有限制x,y的值在image的height和width之间，导致填充越界
	- 在flood fill修改之后也对画圆的部分1添加了限制
	- 由于划线的部分只要两个点都在image中，就不会出现越界的情况，所以没有特殊处理
2. 画圆算法错误：
	- 只复现了1/4的圆

#### 未解决的问题
无

#### 建议
无